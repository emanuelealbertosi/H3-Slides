# Copy to .env and set a random local secret. Never commit .env.
SEARXNG_SECRET=replace-with-a-random-secret
H3_SEARCH_PORT=8080
# Override with a tested official tag or digest for reproducible releases.
SEARXNG_IMAGE=docker.io/searxng/searxng:latest
